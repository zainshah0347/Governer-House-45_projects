// No Users

let userName : string[]= ["zain","bilal","saad"];
userName.pop();
userName.pop();
userName.pop();

if (userName.length === 0) {
    console.log("we need to find some users");
}
