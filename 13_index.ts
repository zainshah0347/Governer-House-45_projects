// Your Own Array

let favoriteTransportation: string[] = ["car", "motorcycle", "bicycle", "boat"];

// Print statements about each item in the list
for (let i = 0; i < favoriteTransportation.length; i++) {
    console.log(`I would like to own a ${favoriteTransportation[i]}.`);
}
