// Greetings

let namess: string[] = ["Zain", "Bilal", "Saad", "Umer"];
let messages: string = "Hello, ";

// Print a personalized message to each person in the array
for (let i = 0; i < namess.length; i++) {
    console.log(messages + namess[i] + "!");
}
