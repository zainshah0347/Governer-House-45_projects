// Greetings
var namess = ["Zain", "Bilal", "Saad", "Umer"];
var messages = "Hello, ";
// Print a personalized message to each person in the array
for (var i = 0; i < namess.length; i++) {
    console.log(messages + namess[i] + "!");
}
