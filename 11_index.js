// Names
var pername = ["Zain", "Bilal", "Saad", "Umer"];
// Print each person's name by accessing each element in the list, one at a time
for (var i = 0; i < pername.length; i++) {
    console.log(pername[i]);
}
